import React, { useState } from 'react';
import { Sparkles, Check, Zap, ShieldCheck, Mic, FileSpreadsheet, Bot, Crown, ArrowRight, X, Lock, CheckCircle2, Clock } from 'lucide-react';
import { motion, AnimatePresence } from 'motion/react';
import { AppSettings, AuthUser, Note } from '../types';
import { createCheckoutSession, createPortalSession } from '../lib/billingService';
import { isFirebaseConfigured } from '../lib/firebase';

interface PricingViewProps { settings: AppSettings; onUpdateSettings: (newSettings: AppSettings) => void; onClose?: () => void; isModal?: boolean; user?: AuthUser | null; isPro?: boolean; notes?: Note[]; }
const FREE_MONTHLY_MINUTES = 300;
const PRO_MONTHLY_MINUTES = 1500;
function getMinutesUsedThisMonth(notes: Note[]): number { const now = new Date(); const totalSeconds = notes.filter((n) => { if (n.type !== 'voice' || !n.durationSeconds) return false; const created = new Date(n.createdAt); return created.getMonth() === now.getMonth() && created.getFullYear() === now.getFullYear(); }).reduce((sum, n) => sum + (n.durationSeconds || 0), 0); return Math.round(totalSeconds / 60); }
export const PricingView: React.FC<PricingViewProps> = ({ settings, onUpdateSettings, onClose, isModal = false, user, isPro: isProOverride, notes = [] }) => {
 const [billingCycle, setBillingCycle] = useState<'monthly' | 'annual'>('annual'); const [isUpgrading, setIsUpgrading] = useState(false); const [upgradeSuccess, setUpgradeSuccess] = useState(false); const [billingError, setBillingError] = useState(''); const isPro = isProOverride ?? (settings.isProPlan ?? false); const billingIsLive = isFirebaseConfigured; const minutesUsed = getMinutesUsedThisMonth(notes); const monthlyCap = isPro ? PRO_MONTHLY_MINUTES : FREE_MONTHLY_MINUTES; const usagePercent = Math.min(100, Math.round((minutesUsed / monthlyCap) * 100));
 const handleStartTrial = async () => { setBillingError(''); if (billingIsLive) { if (!user) { setBillingError('Please sign in first — go back and log in, then come try again.'); return; } setIsUpgrading(true); const result = await createCheckoutSession(user.id, user.email, billingCycle); if (result.configured && result.url) { window.location.href = result.url; return; } setIsUpgrading(false); } setIsUpgrading(true); setTimeout(() => { setIsUpgrading(false); setUpgradeSuccess(true); onUpdateSettings({ ...settings, isProPlan: true, autoJoinMeetings: false }); setTimeout(() => { if (onClose) onClose(); }, 2000); }, 1200); };
 const handleManageBilling = async () => { if (!user) return; setBillingError(''); setIsUpgrading(true); const result = await createPortalSession(user.id, user.email); setIsUpgrading(false); if (result.configured && result.url) window.location.href = result.url; else if (!billingIsLive) onUpdateSettings({ ...settings, isProPlan: false }); else setBillingError(result.error || 'Could not open billing portal.'); };
 const proPriceMonthly = 7.99; const proPriceAnnualMonthly = 5.83; const proAnnualTotal = 69.99;
 const proFeatures = [{ title: `${PRO_MONTHLY_MINUTES.toLocaleString()} Transcription Minutes / mo`, desc: '5x the free tier, plus multi-hour audio recording uploads', icon: Mic }, { title: 'Advanced AI Personas', desc: 'Tailor summaries for Student, Executive, Creative, or Developer', icon: Bot }, { title: 'Direct Exports to Notion, Docs & Boards', desc: 'One-click export as PDF, Markdown, and direct workspace sync', icon: FileSpreadsheet }];
 return (<div className={`w-full max-w-4xl mx-auto ${isModal ? 'p-2 sm:p-4' : 'pb-24 pt-2'}`}><div className="bg-white border border-slate-200/80 rounded-3xl p-5 sm:p-8 shadow-xl relative overflow-hidden"><div className="absolute -top-24 -right-24 w-72 h-72 bg-gradient-to-br from-indigo-200/40 via-purple-200/30 to-transparent rounded-full blur-3xl pointer-events-none"/><div className="absolute -bottom-24 -left-24 w-72 h-72 bg-gradient-to-tr from-purple-200/30 via-indigo-200/20 to-transparent rounded-full blur-3xl pointer-events-none"/>{isModal && onClose && (<button onClick={onClose} className="absolute top-4 right-4 p-2 rounded-2xl bg-slate-100 hover:bg-slate-200 text-slate-500 hover:text-slate-800 transition z-10 cursor-pointer" title="Close"><X className="w-5 h-5"/></button>)}<AnimatePresence>{upgradeSuccess && (<motion.div initial={{opacity:0,scale:0.95}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:0.95}} className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-950/60 backdrop-blur-sm"><div className="bg-white rounded-3xl p-6 sm:p-8 max-w-sm w-full shadow-2xl flex flex-col items-center justify-center text-center space-y-4 border border-slate-100 relative"><div className="w-16 h-16 rounded-full bg-emerald-100 text-emerald-600 flex items-center justify-center shadow-lg shadow-emerald-100"><CheckCircle2 className="w-10 h-10 animate-bounce"/></div><div className="space-y-1.5"><h3 className="text-xl font-extrabold text-slate-900 tracking-tight">Welcome to Pro! 🎉</h3><p className="text-xs sm:text-sm font-medium text-slate-600">Your 7-day free trial is active. {PRO_MONTHLY_MINUTES.toLocaleString()} monthly transcription minutes, advanced AI personas, and expanded exports are now enabled.</p></div><span className="text-xs font-bold text-indigo-700 bg-indigo-50 px-3.5 py-1 rounded-full border border-indigo-100">PRO ACTIVE</span></div></motion.div>)}</AnimatePresence><div className="text-center space-y-3 max-w-xl mx-auto relative z-10"><div className="inline-flex items-center gap-2 px-3.5 py-1 rounded-full bg-gradient-to-r from-indigo-50 via-purple-50 to-indigo-50 text-indigo-700 text-xs font-extrabold tracking-wide border border-indigo-100 shadow-2xs"><Sparkles className="w-3.5 h-3.5 text-indigo-600 animate-pulse"/><span>VOXNOTE PRO</span></div><h1 className="text-2xl sm:text-4xl font-extrabold text-slate-900 tracking-tight leading-tight">Upgrade to Pro</h1><p className="text-xs sm:text-sm font-medium text-slate-500 leading-relaxed">Unlock {PRO_MONTHLY_MINUTES.toLocaleString()} transcription minutes/mo, advanced AI personas, and expanded exports.</p><div className="pt-2 flex items-center justify-center"><div className="bg-slate-100 p-1 rounded-2xl flex items-center border border-slate-200/80 relative shadow-inner"><button type="button" onClick={()=>setBillingCycle('monthly')} className={`relative px-4 py-2 rounded-xl text-xs font-bold transition-all duration-200 cursor-pointer ${billingCycle==='monthly'?'bg-white text-indigo-700 shadow-sm':'text-slate-500 hover:text-slate-800'}`}>Monthly Billing</button><button type="button" onClick={()=>setBillingCycle('annual')} className={`relative px-4 py-2 rounded-xl text-xs font-bold transition-all duration-200 flex items-center gap-1.5 cursor-pointer ${billingCycle==='annual'?'bg-white text-indigo-700 shadow-sm':'text-slate-500 hover:text-slate-800'}`}><span>Annual Billing</span><span className="px-2 py-0.5 rounded-full text-[10px] font-extrabold bg-emerald-500 text-white shadow-2xs">SAVE</span></button></div></div><div className="mt-5 grid grid-cols-1 sm:grid-cols-2 gap-4 text-left"><div className="rounded-3xl border border-slate-200 bg-slate-50/80 p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="text-sm font-extrabold text-slate-900">Pro for Individuals</h3><p className="text-[11px] text-slate-500 mt-1">For power users & professionals</p></div><div className="rounded-2xl bg-indigo-100 p-2 text-indigo-700"><Crown className="w-5 h-5"/></div></div><div className="mt-4"><div className="flex items-end gap-1"><span className="text-4xl font-black tracking-tight text-slate-950">${billingCycle==='monthly'?proPriceMonthly.toFixed(2):proPriceAnnualMonthly.toFixed(2)}</span><span className="text-xs font-bold text-slate-500 mb-1">/month</span></div>{billingCycle==='annual'&&<p className="text-[10px] font-bold text-emerald-600 mt-1">Billed annually as ${proAnnualTotal.toFixed(2)}/yr</p>}</div><div className="mt-5 space-y-2.5">{proFeatures.map((feature)=>{const Icon=feature.icon;return <div key={feature.title} className="flex gap-2.5"><Icon className="w-4 h-4 text-indigo-600 shrink-0 mt-0.5"/><div><p className="text-[11px] font-extrabold text-slate-800">{feature.title}</p><p className="text-[10px] text-slate-500">{feature.desc}</p></div></div>})}</div>{isPro?(<button onClick={handleManageBilling} disabled={isUpgrading} className="mt-5 w-full rounded-2xl bg-slate-950 text-white py-3 text-xs font-extrabold hover:bg-slate-800 disabled:opacity-50">{isUpgrading?'Opening…':'Manage Pro Subscription'}</button>):(<button onClick={handleStartTrial} disabled={isUpgrading} className="mt-5 w-full rounded-2xl bg-indigo-600 text-white py-3 text-xs font-extrabold hover:bg-indigo-500 disabled:opacity-50">{isUpgrading?'Redirecting to Paystack…':'Start 7-Day Free Trial'}</button>)}{billingError&&<div className="mt-3 text-[10px] font-semibold text-rose-600 text-center">{billingError}</div>}</div></div></div></div>);
};